// Social links shown in the footer.
//
// Empty by default and rendered only when set — an inert or invented link is
// worse than none. Same rule as DASHBOARD_VIDEO in app/dashboard-media.ts.
//
// TO FILL IN: paste the full profile URL, e.g.
//   export const SOCIAL = { tiktok: 'https://www.tiktok.com/@tonpseudo' };
//
// The landing already states the TikTok following in its proof strip, so this
// is the one link a visitor is most likely to look for — it lets them check
// the figure themselves, which is exactly what an honest claim should allow.
export const SOCIAL: { tiktok: string | null } = {
  tiktok: null,
};
